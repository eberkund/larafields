<div class="form-group">
    <label for="field-{{ $field->id }}">{{ $field->label }}</label>

    <textarea class="form-control"
              id="field-{{ $field->id }}"
              rows="3"></textarea>
</div>
